<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="@drawable/create_compte_photo">

    <!-- Cadre semi-transparent pour lisibilité -->
    <LinearLayout
        android:id="@+id/container"
        android:orientation="vertical"
        android:background="#80000000"
        android:padding="24dp"
        android:gravity="center_horizontal"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Titre -->
        <TextView
            android:id="@+id/tv_verif_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Vérification"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="#2E7D32"
            android:layout_marginBottom="12dp"
            android:gravity="center"/>

        <!-- Sous-titre -->
        <TextView
            android:id="@+id/tv_verif_desc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Un code de 6 chiffres vous a été envoyé.\nVeuillez le saisir ci-dessous."
            android:textColor="#FFFFFF"
            android:gravity="center"
            android:textSize="15sp"
            android:layout_marginBottom="24dp"/>

        <!-- Champ du code -->
        <EditText
            android:id="@+id/et_code"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:background="@android:drawable/edit_text"
            android:hint="Code à 6 chiffres"
            android:textColor="#FFFFFF"
            android:textColorHint="#BDBDBD"
            android:gravity="center"
            android:inputType="numberPassword"
            android:textSize="18sp"
            android:padding="10dp"
            android:layout_marginBottom="24dp"/>

        <!-- Bouton Vérifier -->
        <Button
            android:id="@+id/btn_verify"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:text="Vérifier"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="@android:color/white"
            android:backgroundTint="#2E7D32"
            app:cornerRadius="20dp"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
